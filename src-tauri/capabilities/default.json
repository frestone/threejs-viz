{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Three.js Viz 桌面外壳默认权限：核心 + 文件对话框（打开本地 .mcap 拿绝对路径）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}